- name: playbook
  hosts: webservers
  become: yes
  become_user: root


  tasks:
        - name: install docker
          yum:
                  name: docker
                  state: present
        - name: start docker
          service:
                  name: docker
                  state: started
